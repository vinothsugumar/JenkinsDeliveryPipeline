apiVersion: v1
kind: Service
metadata:
    name: "devopskubernetescluster-726c"
    labels:
        app: "devopskubernetescluster-726c"
spec:
    type: LoadBalancer
    ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
    selector:
        app: "devopskubernetescluster-726c"